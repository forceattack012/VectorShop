
using Microsoft.AspNetCore.SignalR;

namespace Basket.Domain.Hubs
{
    public class BasketHub : Hub
    {
        //public async Task SendBasketCartEvent(string user, BasketCartEvent basket)
        //{
        //    await Clients.User(user).SendAsync("sendBasketCartEvent", basket);
        //}
    }
}
